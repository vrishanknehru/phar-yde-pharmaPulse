name: pharmapulse
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  eva_icons_flutter: ^3.1.0
  provider: ^6.1.2
  google_maps_flutter: ^2.6.1
  geolocator: ^12.0.0
  http: ^1.2.1
  image_picker: ^1.1.2  # For camera and gallery access
  file_picker: ^8.0.5   # For picking PDF files
  flutter_dotenv: ^5.1.0
  url_launcher: ^6.3.2
  flutter_polyline_points: ^3.0.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env